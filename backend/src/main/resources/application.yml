spring:
  application:
    name: backend

  profiles:
    active: dev

  servlet:
    context-path: /colegio/api/v1

  config:
    import: optional:file:.env[.properties]

springdoc:
  swagger-ui:
    path: /docs
